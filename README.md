# Max-Product-of-3-Numbers-Recursive-vs-Non-Recursive
A Python algorithm analysis project comparing two approaches to find the maximum product of any triplet — Iterative O(n²) using insertion sort vs Recursive O(n³) using combination enumeration — with correctness tests and performance benchmarks.
